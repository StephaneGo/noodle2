server:
  port: 5001

spring:
  application:
    name: service-catalogue-tests

  datasource:
    url: jdbc:mysql://localhost:3306/catalogue-tests-db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true
    properties:
      hibernate:
        format_sql: true
